#include <bits/stdc++.h>
using namespace std;

int main()
{
  int n, q, val;
  cin >> n >> q;
  vector<long long> a(n + 1, 0),
      part(n + 2, 0);

  // get array input.
  for (int i = 1; i <= n; i++)
    cin >> a[i];

  while (q--)
  {
    int l, h;
  }
}