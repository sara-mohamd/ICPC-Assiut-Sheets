#include <bits/stdc++.h>
using namespace std;

int main()
{
  // int n, k;
  // long long ctr = 0;
  // cin >> n >> k;
  // vector<int> a(n, 0);
  // for (int i = 0; i < n; i++)
  //   cin >> a[i];
  // for (int i = 0; i < n; i++)
  // {
  //   for (int j = i + 1; j < n; j++)
  //   {
  //     if ((a[i] + a[j]) % k == 0)
  //     {
  //       ctr += 2;
  //       a.erase(a.begin() + j);
  //       n--;
  //       break;
  //     }
  //   }
  // }
  cout << 450 % 100 << endl;
}