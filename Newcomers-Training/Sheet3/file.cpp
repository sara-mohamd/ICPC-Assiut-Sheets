#include <iostream>
using namespace std;

int main()
{
  int testCases;
  cin >> testCases;
  while (testCases)
  {
    int max, size;
    cin >> size;
    int arr[size];

    for (int i = 0; i < size; i++)
      cin >> arr[i];

    int max_size = 0;
    for (int i = 0; i < size; i++)
    {
      int j = i, max = 0;
      while (j < size)
      {

        j--;
      }
    }
    cout << endl;
    testCases--;
  }
}