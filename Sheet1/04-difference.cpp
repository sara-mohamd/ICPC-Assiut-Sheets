#include <iostream>
using namespace std;
int main()
{
  long long int a, b, c, d;

  cin >> a >> b >> c >> d;
  // difference = (a * b) - (c * d)

  cout << "Difference = " << (a * b) - (c * d);
  return 0;
}